# Speech Service Configuration

# Phase A (API-based) or Phase B (local)
STT_PROVIDER=api  # Options: api, local
TTS_PROVIDER=api  # Options: api, local

# Phase A - Groq API (for Whisper large-v3-turbo)
GROQ_API_KEY=your_groq_api_key_here

# Phase A - Google Cloud TTS
GOOGLE_TTS_CREDENTIALS_PATH=/path/to/google-credentials.json
GOOGLE_TTS_PROJECT_ID=your-project-id

# Phase B - Local models (future)
# WHISPER_MODEL_PATH=/path/to/whisper-model
# TTS_MODEL_PATH=/path/to/tts-model
